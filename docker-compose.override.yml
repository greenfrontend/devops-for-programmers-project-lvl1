version: "3.7"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - "NODOS_HOST=0.0.0.0"
    ports:
      - "0.0.0.0:8080:8080"
    command: npm start
    volumes:
      - ./:/app
